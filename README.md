# A7R5Scripts + vclip (merge in progress)

This repository now contains both:

- Photo workflow (`batch_shrink.py`, `fix_photo_time.py`)
- Video split / merge / shrink (`vclip`)

They will be unified into a single `mediakit` CLI.
